{
  "name": "pulselimiter",
  "version": "0.1.0",
  "description": "⚡ Tiny, production-ready rate limiter for Node.js APIs (Token Bucket + Express middleware)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "dev:test": "vitest",
    "prepare": "npm run build"
  },
  "keywords": [
    "rate-limit",
    "limiter",
    "express",
    "middleware",
    "security",
    "token-bucket"
  ],
  "author": "p4triot1905",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
